信息分组的默认容器。**克制使用**：一屏两三个面板就够，其余内容直接放在页面底色上用细线分隔。

```jsx
<Card eyebrow="Generation · 24h" title="发电负荷与供汽负荷">…</Card>
<Card variant="outlined" tone="bare" padding={24}>与页面同色时才需要描边</Card>
```

- 默认无描边无阴影。**不要同时给描边和阴影**——那是上一版最主要的视觉噪音来源。
- 标题是宋体 24px；需要更小的标题说明这块内容不该是面板。
- `accent` 色条只表示「当前选中」，禁止当装饰。
