品牌展示面与长页面的章节起头；产品界面的页头请用普通标题，不要用它。

```jsx
<SectionHead index="02 / 04" eyebrow="Five entities" title="五家公司，一个共生体"
  lede="共用一套设计语法与治理标准，各自生长出独立的色彩身份。" />
<SectionHead eyebrow="The loop" title="一条闭环，四段价值" tone="ink" />
```

- 标题 200 字重的宋体是全系统最大的字——一页里最多出现一次同级标题。
- 印章方块用 `--seal`（朱砂），是唯一允许出现的装饰元素，且只此一枚。
